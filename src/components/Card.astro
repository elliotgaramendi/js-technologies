---
const {
  data: { id, title, slug, subtitle, description, image },
} = Astro.props;
---

<li
  class="rounded-2xl overflow-hidden shadow-sm shadow-sky-500 flex flex-col transition-shadow hover:shadow-md hover:shadow-sky-500"
>
  <a href=`#${id}-${slug}`>
    <img
      src={image}
      alt={title}
      width="640"
      height="360"
      loading="lazy"
      class="w-full h-48 object-cover"
    />
  </a>
  <div class="h-full p-3 bg-zinc-800 flex flex-col gap-2">
    <h2 class="text-lg font-bold">{title}</h2>
    <h3 class="text-zinc-400 font-semibold">{subtitle}</h3>
    <p class="text-sm">{description}</p>
    <a
      href="#"
      class="px-2 py-0.5 mt-auto border-2 rounded-lg border-sky-500 bg-sky-500 font-semibold text-center transition-all hover:brightness-125"
    >
      Learn
    </a>
  </div>
</li>
